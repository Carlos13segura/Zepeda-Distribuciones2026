import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-not-found.spec',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './not-found.spec.html',
  styleUrls: ['./not-found.spec.scss']
})
export class NotFoundSpecComponent {
}
